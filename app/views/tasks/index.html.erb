<div class="min-h-screen bg-bg-soft">
  <div class="container mx-auto px-4 py-8">
    <div class="max-w-4xl mx-auto">
      <!-- ヘッダー部分 -->
      <div class="mb-8">
        <div class="flex flex-col md:flex-row md:items-center md:justify-between">
          <div class="mb-4 md:mb-0">
            <h1 class="text-2xl md:text-3xl font-bold text-primary">
              <i class="fas fa-tasks mr-2"></i>タスク管理
            </h1>
            <p class="text-gray-600 mt-1">
              <i class="fas fa-user mr-1"></i><%= current_user.email %>
            </p>
          </div>

          <div class="flex items-center space-x-2 text-sm text-gray-500">
            <span><i class="fas fa-calendar-day mr-1"></i><%= Time.current.strftime("%Y年%m月%d日") %></span>
            <span class="mx-2">|</span>
            <span><i class="fas fa-list-check mr-1"></i>タスク数: <%= @tasks.count %></span>
          </div>
        </div>
      </div>

      <!-- タスク追加フォーム -->
      <%= turbo_frame_tag "task_form" do %>
        <%= render "form" %>
      <% end %>

      <!-- タスク一覧 -->
      <div class="card p-6">
        <h2 class="text-lg font-semibold text-primary mb-4 flex items-center">
          <i class="fas fa-list-check mr-2"></i>タスク一覧
          <span class="ml-2 text-xs bg-accent text-white px-2 py-1 rounded-full"><%= @tasks.count %></span>
        </h2>

        <%= turbo_frame_tag "tasks" do %>
          <% if @tasks.any? %>
            <div class="divide-y">
              <% @tasks.each do |task| %>
                <%= render task %>
              <% end %>
            </div>
          <% else %>
            <div class="py-8 text-center">
              <div class="w-16 h-16 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-4">
                <i class="fas fa-clipboard-list text-gray-400 text-2xl"></i>
              </div>
              <p class="text-gray-500">タスクがありません。新しいタスクを追加してください。</p>
            </div>
          <% end %>
        <% end %>
      </div>

      <!-- ヒント -->
      <div class="mt-8 bg-primary bg-opacity-5 border-l-4 border-primary p-4 rounded-r">
        <h3 class="font-semibold text-primary flex items-center">
          <i class="fas fa-lightbulb text-accent mr-2"></i>ヒント
        </h3>
        <p class="text-white text-sm mt-1">
          タスクを完了するには、チェックボックスをクリックします。タスクを削除するには、削除ボタンをクリックします。
        </p>
      </div>
    </div>
  </div>
</div>
